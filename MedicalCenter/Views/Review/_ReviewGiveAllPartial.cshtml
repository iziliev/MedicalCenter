@model AllGiveReviewViewModel

<tr>
    <th scope="row" class="text-center">@Model.DoctorFullName</th>
    <th scope="row" class="text-center">
        <textarea class="col-12 " rows="5" readonly>@Model.Content</textarea>
    </th>
    <th scope="row" class="text-center">@Model.Rating</th>
    <th scope="row" class="text-center">@Model.CreatedOn</th>
</tr>
